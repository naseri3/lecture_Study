// ===============================
// 📌 오늘의 JS 문제 — 중급 난이도 ⭐⭐⭐⭐
// ===============================


// --------------------------------
// 문제 1 — 특정 조건 필터링 + for…of
// --------------------------------
// students 배열에서 점수가 80점 이상인 학생의 이름만 출력하세요.
// 출력은 console.log로 한 줄씩 출력.
// (filter 사용 ❌)

const students = [
    { name: "김민준", score: 76 },
    { name: "이서연", score: 88 },
    { name: "박지호", score: 92 },
    { name: "최윤아", score: 67 },
    { name: "정하늘", score: 81 },
  ];

let student = []

for (const name of students) {
    if(name.score >= 80) {
        student.push(name.name);
    }
}
console.log(student);
  
  // 출력 예:
  // 이서연
  // 박지호
  // 정하늘
  
  
  
  console.log("--------------------------------");
  
  
  // --------------------------------
  // 문제 2 — for + 인덱스 활용
  // --------------------------------
  // numbers 배열에서 "짝수"만 골라서,
  // newArr 에 담고 최종적으로 newArr 출력하세요.
  
  const numbers = [11, 42, 5, 20, 99, 101, 30, 4];
  let newArr = [];

  for(let i = 0; i < numbers.length; i++) {
    if(numbers[i] % 2 === 0) {
        newArr.push(numbers[i]);
    }
  }
  console.log(newArr);
  
  // 출력 예: [42, 20, 30, 4]
  
  
  
  console.log("--------------------------------");
  
  
  // --------------------------------
  // 문제 3 — for…in + 객체 값 찾기
  // --------------------------------
  // user 객체 내부 값 중,
  // 타입이 "string"인 값만 출력하세요.
  
  const user = {
    id: 101,
    name: "홍길동",
    email: "hong@test.com",
    age: 34,
    job: "Security Guard",
  };
  
  for (const key in user) {
    if(typeof user[key] === "string") {
        console.log(user[key]);
    }
  }
  
  // 출력 예:
  // 홍길동
  // hong@test.com
  // Security Guard
  
  
  
  console.log("--------------------------------");
  
  
  // --------------------------------
  // 문제 4 — forEach + 합계 구하기
  // --------------------------------
  // salaries 배열의 모든 값을 더해 총합을 출력하세요.
  
  const salaries = [320, 450, 280, 510, 390];
  let sum = 0;

  salaries.forEach(element => {
    sum += element;
  });
  console.log(sum);
  
  // 출력 예: 총 급여: 1950
  
  
  
  console.log("--------------------------------");
  
  
  // --------------------------------
  // 문제 5 — 혼합 조건 문제 (for…of + includes)
  // --------------------------------
  // jobs 배열 중 title에 "관리"라는 단어가 포함된
  // 공고만 newJobs 배열에 저장 후 console.log로 출력하세요.
  
  const jobs = [
    { id: 1, title: "경비원" },
    { id: 2, title: "주차 관리 직원" },
    { id: 3, title: "시설기사" },
    { id: 4, title: "안전 관리 감독" },
    { id: 5, title: "청소 미화" },
  ];
  let newJobs = [];

  for (const job of jobs) {
    if(job.title.includes("관리")) {
        newJobs.push(job.title);
    }
  }
  console.log(newJobs);

  // 출력 예: [
  //   "주차 관리 직원",
  //   "안전 관리 감독"
  // ]
  